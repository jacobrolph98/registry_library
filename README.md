This library adds a type called Definition, which is a struct that contains at least an ID field.
Definitions are intended to contain stats/data for various gameplay elements (Items, weapons, projectiles, etc)
Using registries allows for these data stores to read modded content on top of base game.
